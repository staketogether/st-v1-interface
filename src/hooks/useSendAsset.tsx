import { Asset } from "@/types/Asset"

interface SendAssetProps {
    walletTo: string
    amount: string
    asset: Asset
}

export default function useSendAsset({ walletTo, amount, asset }: SendAssetProps) {



}